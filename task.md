# 実装タスクリスト

## サーバー基盤
- [x] Makefileの作成（`$(NAME)`, `all`, `clean`, `fclean`, `re` ルールを含む）
- [x] Makefileが `-Wall -Wextra -Werror` フラグでコンパイルできることを確認
- [x] Makefileが `-std=c++98` フラグでコンパイルできることを確認
- [x] コマンドライン引数（ポート、パスワード）のパース処理を実装
- [x] TCP/IP (v4またはv6) ソケットの初期化と設定
- [x] サーバーが指定されたポートでリッスンできるように実装
- [ ] 接続パスワードの検証ロジックを実装
- [x] `poll()` (または同等のもの) を使用したI/O多重化の基本構造を実装
- [x] 新しいクライアント接続を受け入れる処理を実装
- [x] すべてのI/O操作を非ブロッキングに設定
- [x] クライアントからのデータ読み取り処理を実装
- [x] クライアントへのデータ書き込み処理を実装

## クライアント管理とコマンドパース
- [x] `Client` クラスの設計と実装（認証状態、ニックネーム、ユーザー名などを保持）
- [x] クライアントからのメッセージをIRCコマンドとしてパースする機構の実装
- [x] 各クライアントの認証状態を管理するロジックの実装

## IRCプロトコルとコマンド
- [ ] `PASS` コマンドの実装（認証）
- [ ] `NICK` コマンドの実装（ニックネーム設定）
- [ ] `USER` コマンドの実装（ユーザー名設定）
- [ ] 認証フロー（PASS -> NICK -> USER）を実装
- [ ] `JOIN` コマンドの実装（チャンネル参加）
- [ ] `PRIVMSG` コマンドの実装（プライベートメッセージ送受信）
- [ ] チャンネル内の全クライアントへのメッセージ転送ロジックを実装
- [ ] オペレーターと一般ユーザーの概念を実装

## チャンネルオペレーターコマンド
- [ ] `KICK` コマンドの実装
- [ ] `INVITE` コマンドの実装
- [ ] `TOPIC` コマンドの実装（トピック変更・表示）
- [ ] `MODE` コマンドの基本構造を実装
- [ ] `MODE +i/-i` (Invite-only) の実装
- [ ] `MODE +t/-t` (Topic restrictions) の実装
- [ ] `MODE +k/-k` (Channel key) の実装
- [ ] `MODE +o/-o` (Operator privilege) の実装
- [ ] `MODE +l/-l` (User limit) の実装

## 品質とテスト
- [ ] プログラムがいかなる状況でもクラッシュしないことを確認
- [ ] 参照IRCクライアントを選定し、接続テストを行う
- [ ] `nc` を使用したテスト (`nc -C 127.0.0.1 6667`) でサーバーが正しくデータを処理することを確認
- [ ] 部分的なデータ受信、低帯域幅などのエラーケースを考慮したテスト
- [ ] コードのクリーンアップとリファクタリング（継続的に実施）

## MacOS固有
- ~~[ ] MacOS環境での `fcntl(fd, F_SETFL, O_NONBLOCK);` の使用を確認~~
