- [ ] `Client` クラスまたは構造体を作成し、クライアントのニックネーム、ユーザー名、認証状態、参加しているチャンネルなどの情報を管理する。
- [ ] `IrcServer` クラスに、`std::map<int, Client>` のようなコンテナを追加して、ファイルディスクリプタ (`int`) とクライアント情報を関連付ける。
- [ ] `processMessage` 関数を拡張し、クライアントから受信したメッセージをIRCコマンドとパラメータに分割するパーサーを実装する。
    - [ ] 受信メッセージをスペースで分割して、コマンドとパラメータのリストに変換する。
    - [ ] コマンド部分を抽出する。
    - [ ] パラメータ部分を抽出する。
    - [ ] `Command` 構造体またはクラスを定義して、パースしたコマンドとパラメータを格納する。
- [ ] コマンドの大文字と小文字を区別しないように処理する。
- [ ] `NICK` コマンドを実装する。
    - [ ] クライアントのニックネームを設定する。
    - [ ] ニックネームが既に使用されている場合は、`ERR_NICKNAMEINUSE` (433) エラーを返す。
- [ ] `USER` コマンドを実装する。
    - [ ] クライアントのユーザー名、ホスト名、サーバー名、実名を設定する。
- [ ] `PASS` コマンドを実装する。
    - [ ] サーバーへの接続パスワードを検証する。
- [ ] クライアントが `NICK` と `USER` の両方を送信したら、認証済みとみなし、ウェルカムメッセージ (`RPL_WELCOME` 001) を送信する。
- [ ] `Channel` クラスまたは構造体を作成し、チャンネル名、トピック、参加しているクライアントのリストなどを管理する。
- [ ] `IrcServer` クラスに、`std::map<std::string, Channel>` のようなコンテナを追加して、チャンネル名とチャンネル情報を関連付ける。
- [ ] `JOIN` コマンドを実装する。
    - [ ] クライアントをチャンネルに参加させる。
    - [ ] チャンネルが存在しない場合は、新しく作成する。
    - [ ] 成功したら、クライアントに `JOIN` メッセージを送信し、チャンネルの他のメンバーに新しいユーザーの参加を通知する。
    - [ ] チャンネルのトピック (`RPL_TOPIC` 332) とユーザーリスト (`RPL_NAMREPLY` 353) をクライアントに送信する。
- [ ] `PART` コマンドを実装する。
    - [ ] クライアントをチャンネルから退出させる。
    - [ ] チャンネルの他のメンバーに、ユーザーの退出を通知する。
- [ ] `PRIVMSG` コマンドを実装する。
    - [ ] ターゲットがチャンネルの場合、そのチャンネルに参加しているすべてのクライアント（送信者を除く）にメッセージを転送する。
    - [ ] ターゲットがユーザーのニックネームの場合、そのユーザーにプライベートメッセージを送信する。
    - [ ] ターゲットが存在しない場合は、`ERR_NOSUCHNICK` (401) または `ERR_NOSUCHCHANNEL` (403) エラーを返す。
- [ ] `PING`/`PONG` コマンドを実装し、クライアントとの接続がアクティブであることを確認する。
- [ ] `QUIT` コマンドを実装し、クライアントが切断する際に、そのクライアントが参加しているすべてのチャンネルに通知する。
- [ ] `disconnectClient` 関数を拡張し、クライアントが切断した際に、関連するすべてのチャンネルからそのクライアントを削除する。
- [ ] IRCプロトコルで定義されている数値応答（RPL\_*）とエラー応答（ERR\_*）を送信するためのヘルパー関数を作成する。