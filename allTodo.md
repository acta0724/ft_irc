# ToDo List

1. プログラムはいかなる状況でもクラッシュせず、予期せず終了しないこと。
2. ソースファイルをコンパイルするMakefileを提出すること。不要な再リンクを行わないこと。
3. Makefileには少なくとも `$(NAME)`, `all`, `clean`, `fclean`, `re` のルールを含めること。
4. `-Wall -Wextra -Werror` フラグを使用してC++でコードをコンパイルすること。
5. コードはC++98標準に準拠すること。`-std=c++98` フラグを追加してもコンパイルできること。
6. 可能な場合は常にC++の機能を使用すること（例: `<string.h>` よりも `<cstring>` を選択）。C関数も許可されるが、可能な場合はC++バージョンを優先すること。
7. 外部ライブラリおよびBoostライブラリは禁止。
8. IRCサーバーをC++98標準を使用して開発すること。
9. IRCクライアントを開発しないこと。
10. サーバー間通信を実装しないこと。
11. 実行ファイルは `./ircserv <port> <password>` の形式で実行できること。
    - `port`: IRCサーバーが着信IRC接続をリッスンするポート番号。
    - `password`: 接続パスワード。
12. 複数のクライアントを同時に処理できること（ハングしないこと）。
13. forkは禁止。すべてのI/O操作は非ブロッキングであること。
14. すべての操作（読み取り、書き込み、リッスンなど）を処理するために、1つの `poll()` (または同等のもの) のみを使用できること。
15. 参照クライアントを選択し、評価プロセス中に使用できるようにすること。
16. 参照クライアントがエラーなくサーバーに接続できること。
17. クライアントとサーバー間の通信はTCP/IP (v4またはv6) 経由で行われること。
18. 参照クライアントをサーバーで使用する際、公式IRCサーバーを使用するのと同様の体験を提供すること。ただし、以下の機能のみを実装すること。
    - 認証、ニックネームの設定、ユーザー名の設定、チャンネルへの参加、プライベートメッセージの送受信ができること。
    - 1つのクライアントからチャンネルに送信されたすべてのメッセージは、チャンネルに参加している他のすべてのクライアントに転送されること。
    - オペレーターと一般ユーザーが存在すること。
    - チャンネルオペレーターに特有のコマンドを実装すること。
        - **KICK**: クライアントをチャンネルから強制退去させる。
        - **INVITE**: クライアントをチャンネルに招待する。
        - **TOPIC**: チャンネルのトピックを変更または表示する。
        - **MODE**: チャンネルのモードを変更する。
            - **i**: 招待制チャンネルの設定/解除。
            - **t**: TOPICコマンドの制限をチャンネルオペレーターに設定/解除。
            - **k**: チャンネルキー（パスワード）の設定/解除。
            - **o**: チャンネルオペレーター権限の付与/剥奪。
            - **l**: チャンネルのユーザー制限の設定/解除。
19. クリーンなコードを書くこと。
20. MacOSの場合、`fcntl()` を `fcntl(fd, F_SETFL, O_NONBLOCK);` の形式でのみ使用すること。他のフラグは禁止。
21. 部分的なデータ受信、低帯域幅など、考えられるすべてのエラーと問題を検証すること。
22. `nc` を使用したテスト (`nc -C 127.0.0.1 6667`) でサーバーが正しくデータを処理することを確認すること。