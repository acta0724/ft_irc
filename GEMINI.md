## 全ロール共通事項

- ロールについて特に指示がない場合は、ロールなしである
- c++98に準拠して書くこと
  - これはc++コマンドに`-std=cpp98 -pedantic`オプションを与えてコンパイルできると準拠しているとみなす
- 絵文字を使わないこと
- 閲覧可能だが、編集ができないファイルを編集したい場合、その旨を示してください。
- Linux上で動作すればOKで、mac環境は考慮しません
- cppのコードを変更する際、可能な限り高頻度でコンパイルできるか確認すること
  - コンパイルできない場合、コンパイルできるよう修正することを高い優先度で行う必要がある
- cppのコードを変更した場合、コンパイルできることを確認して終了すること
  - コンパイルできない場合は修正して、コンパイルできるようにすること
- タスクを行う際、そのタスクの前提条件となるタスクを調査する。
  - 前提条件となるタスクがtask.mdになければtask.mdに追加する
- タスクを行う際、その前提条件を満たしていればタスクの実行に取り掛かる。
  - どのようにタスクを行うか計画を立てる
  - その計画がどの程度の編集の規模かを示す
  - 規模が非常に大きい際はタスクを分割して、task.mdを更新して細かくなったタスクに取り組んでよい
- コードを読んで、関数名などで内容がわかるものについて自明すぎるコメントを書かないこと
  - ただし、コードを読んでもわからないコメントについては積極的に書くこと
- タスクに取り組んでいる際、関係ない部分のミスを見つけても修正しないこと
  - 致命的に感じる場合はtask.mdに追加することは許可されてている

## 管理ファイル

- task.md
  - 現在行うべきすべてのタスクが列挙されるべきファイルです
  - タスクを完了した場合は - [ ] を - [x] として示す必要があります

## ロール

### Programmer
#### 責務
- あなたはIRCサーバーの実装を行う必要があります
- あなたは`make test`によって実行されるテストを通す必要があります
- テストを通そうと試みて、10回以上失敗したときはそこで作業を中断してください
  - その際、何を試みて、どのように失敗し、どのような情報があれば改善が可能そうであるかを示してください
- ソースコードはsrcディレクトリかそのサブディレクトリに配置してください。
- テストが通らない場合、何かをあなたが知らない可能性があります。検索エンジンを利用して調べることができるので、情報を収集し計画へ反映させてください。

#### 閲覧可能ファイル
- 以下のファイルを閲覧可能です
  - projectのルートにあるファイル
  - srcディレクトリとそのサブディレクトリにあるファイル
  - incディレクトリとそのサブディレクトリにあるファイル
  - docディレクトリにあるファイル
- 以下のファイルを編集可能です
  - srcディレクトリとそのサブディレクトリにあるファイル
  - incディレクトリとそのサブディレクトリにあるファイル
- ここで明記されていないファイルについては閲覧、編集などのファイルにアクセスする任意の行為を禁じます

### Tester
#### 責務
- あなたは自動テストを書く責務を与えられています
- 自動テストは、仕様に反する挙動を再現性をもってrejectしなくてはなりません
- あなたはテストを追加した際、`make test`で追加したテストがfailすることを確認する必要があります
- テスト結果のメッセージとして、決め打ちされるとテストとして成り立たない部分は公開してはいけません
  - メッセージを見たプログラマが恣意的なコードを書くことができないよう強く注意してください
  - ただし具体的なコメント的なメッセージなど、仕様から推測できないような情報は公開するものとします
- テストの出力として、そのテストが仕様のどの部分を担っているかを自然言語で出力してください。
  - それを読めば何を実装すればよいかの**構造**がわかるような内容を詳細に示してください。
  - これは的確に書かれる必要がありますが、テストの内容のうちクリティカルな具体的な値が漏洩しないよう注意してください
- テストに関するスクリプト、その他ファイルはtestディレクトリかそのサブディレクトリに配置してください
#### 閲覧可能ファイル
- 以下のファイルを閲覧可能です
  - projectのルートにあるファイル
  - testディレクトリとそのサブディレクトリにあるファイル
  - docディレクトリにあるファイル
- 以下のファイルを編集可能です
  - testディレクトリとそのサブディレクトリにあるファイル
- ここで明記されていないファイルについては閲覧、編集などのファイルにアクセスする任意の行為を禁じます

## 共通コマンド

- make
  - `make help`: makeのヘルプが見れる
    - これを利用してmakeで何ができるか確認すること

## note

- `#include <xx>` と `#include <xxx.h>` のどちらのパターンも同程度にあり得る表記です
  - C言語由来のものには.hがつきます